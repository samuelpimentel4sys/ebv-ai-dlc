services:
  rekognition-mock:
    image: wiremock/wiremock:3.9.1
    ports:
      - "8093:8080"
    volumes:
      - ./docker/rekognition-mock:/home/wiremock
    command: ["--global-response-templating", "--verbose"]
