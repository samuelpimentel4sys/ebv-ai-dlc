# mock = showcase DTOs | live = Prisma backend (Noah)
VITE_DATA_MODE=live

# Empty = same-origin + Vite proxy → localhost:8080
VITE_API_BASE_URL=

# Optional bearer for when OIDC_ENABLED=true
# VITE_API_BEARER=
